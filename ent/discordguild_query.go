// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/h3mmy/bloopyboi/ent/discordchannel"
	"github.com/h3mmy/bloopyboi/ent/discordguild"
	"github.com/h3mmy/bloopyboi/ent/discordmessage"
	"github.com/h3mmy/bloopyboi/ent/discorduser"
	"github.com/h3mmy/bloopyboi/ent/predicate"
)

// DiscordGuildQuery is the builder for querying DiscordGuild entities.
type DiscordGuildQuery struct {
	config
	ctx                      *QueryContext
	order                    []discordguild.OrderOption
	inters                   []Interceptor
	predicates               []predicate.DiscordGuild
	withMembers              *DiscordUserQuery
	withDiscordMessages      *DiscordMessageQuery
	withGuildChannels        *DiscordChannelQuery
	modifiers                []func(*sql.Selector)
	withNamedMembers         map[string]*DiscordUserQuery
	withNamedDiscordMessages map[string]*DiscordMessageQuery
	withNamedGuildChannels   map[string]*DiscordChannelQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DiscordGuildQuery builder.
func (_q *DiscordGuildQuery) Where(ps ...predicate.DiscordGuild) *DiscordGuildQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *DiscordGuildQuery) Limit(limit int) *DiscordGuildQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *DiscordGuildQuery) Offset(offset int) *DiscordGuildQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *DiscordGuildQuery) Unique(unique bool) *DiscordGuildQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *DiscordGuildQuery) Order(o ...discordguild.OrderOption) *DiscordGuildQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryMembers chains the current query on the "members" edge.
func (_q *DiscordGuildQuery) QueryMembers() *DiscordUserQuery {
	query := (&DiscordUserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(discordguild.Table, discordguild.FieldID, selector),
			sqlgraph.To(discorduser.Table, discorduser.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, discordguild.MembersTable, discordguild.MembersPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDiscordMessages chains the current query on the "discord_messages" edge.
func (_q *DiscordGuildQuery) QueryDiscordMessages() *DiscordMessageQuery {
	query := (&DiscordMessageClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(discordguild.Table, discordguild.FieldID, selector),
			sqlgraph.To(discordmessage.Table, discordmessage.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, discordguild.DiscordMessagesTable, discordguild.DiscordMessagesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGuildChannels chains the current query on the "guild_channels" edge.
func (_q *DiscordGuildQuery) QueryGuildChannels() *DiscordChannelQuery {
	query := (&DiscordChannelClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(discordguild.Table, discordguild.FieldID, selector),
			sqlgraph.To(discordchannel.Table, discordchannel.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, discordguild.GuildChannelsTable, discordguild.GuildChannelsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DiscordGuild entity from the query.
// Returns a *NotFoundError when no DiscordGuild was found.
func (_q *DiscordGuildQuery) First(ctx context.Context) (*DiscordGuild, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{discordguild.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *DiscordGuildQuery) FirstX(ctx context.Context) *DiscordGuild {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DiscordGuild ID from the query.
// Returns a *NotFoundError when no DiscordGuild ID was found.
func (_q *DiscordGuildQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{discordguild.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *DiscordGuildQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DiscordGuild entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DiscordGuild entity is found.
// Returns a *NotFoundError when no DiscordGuild entities are found.
func (_q *DiscordGuildQuery) Only(ctx context.Context) (*DiscordGuild, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{discordguild.Label}
	default:
		return nil, &NotSingularError{discordguild.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *DiscordGuildQuery) OnlyX(ctx context.Context) *DiscordGuild {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DiscordGuild ID in the query.
// Returns a *NotSingularError when more than one DiscordGuild ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *DiscordGuildQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{discordguild.Label}
	default:
		err = &NotSingularError{discordguild.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *DiscordGuildQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DiscordGuilds.
func (_q *DiscordGuildQuery) All(ctx context.Context) ([]*DiscordGuild, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DiscordGuild, *DiscordGuildQuery]()
	return withInterceptors[[]*DiscordGuild](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *DiscordGuildQuery) AllX(ctx context.Context) []*DiscordGuild {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DiscordGuild IDs.
func (_q *DiscordGuildQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(discordguild.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *DiscordGuildQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *DiscordGuildQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*DiscordGuildQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *DiscordGuildQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *DiscordGuildQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *DiscordGuildQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DiscordGuildQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *DiscordGuildQuery) Clone() *DiscordGuildQuery {
	if _q == nil {
		return nil
	}
	return &DiscordGuildQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]discordguild.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.DiscordGuild{}, _q.predicates...),
		withMembers:         _q.withMembers.Clone(),
		withDiscordMessages: _q.withDiscordMessages.Clone(),
		withGuildChannels:   _q.withGuildChannels.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithMembers tells the query-builder to eager-load the nodes that are connected to
// the "members" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DiscordGuildQuery) WithMembers(opts ...func(*DiscordUserQuery)) *DiscordGuildQuery {
	query := (&DiscordUserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMembers = query
	return _q
}

// WithDiscordMessages tells the query-builder to eager-load the nodes that are connected to
// the "discord_messages" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DiscordGuildQuery) WithDiscordMessages(opts ...func(*DiscordMessageQuery)) *DiscordGuildQuery {
	query := (&DiscordMessageClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withDiscordMessages = query
	return _q
}

// WithGuildChannels tells the query-builder to eager-load the nodes that are connected to
// the "guild_channels" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DiscordGuildQuery) WithGuildChannels(opts ...func(*DiscordChannelQuery)) *DiscordGuildQuery {
	query := (&DiscordChannelClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withGuildChannels = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Discordid string `json:"discordid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DiscordGuild.Query().
//		GroupBy(discordguild.FieldDiscordid).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *DiscordGuildQuery) GroupBy(field string, fields ...string) *DiscordGuildGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DiscordGuildGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = discordguild.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Discordid string `json:"discordid,omitempty"`
//	}
//
//	client.DiscordGuild.Query().
//		Select(discordguild.FieldDiscordid).
//		Scan(ctx, &v)
func (_q *DiscordGuildQuery) Select(fields ...string) *DiscordGuildSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &DiscordGuildSelect{DiscordGuildQuery: _q}
	sbuild.label = discordguild.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DiscordGuildSelect configured with the given aggregations.
func (_q *DiscordGuildQuery) Aggregate(fns ...AggregateFunc) *DiscordGuildSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *DiscordGuildQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !discordguild.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *DiscordGuildQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DiscordGuild, error) {
	var (
		nodes       = []*DiscordGuild{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withMembers != nil,
			_q.withDiscordMessages != nil,
			_q.withGuildChannels != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DiscordGuild).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DiscordGuild{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withMembers; query != nil {
		if err := _q.loadMembers(ctx, query, nodes,
			func(n *DiscordGuild) { n.Edges.Members = []*DiscordUser{} },
			func(n *DiscordGuild, e *DiscordUser) { n.Edges.Members = append(n.Edges.Members, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withDiscordMessages; query != nil {
		if err := _q.loadDiscordMessages(ctx, query, nodes,
			func(n *DiscordGuild) { n.Edges.DiscordMessages = []*DiscordMessage{} },
			func(n *DiscordGuild, e *DiscordMessage) { n.Edges.DiscordMessages = append(n.Edges.DiscordMessages, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withGuildChannels; query != nil {
		if err := _q.loadGuildChannels(ctx, query, nodes,
			func(n *DiscordGuild) { n.Edges.GuildChannels = []*DiscordChannel{} },
			func(n *DiscordGuild, e *DiscordChannel) { n.Edges.GuildChannels = append(n.Edges.GuildChannels, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedMembers {
		if err := _q.loadMembers(ctx, query, nodes,
			func(n *DiscordGuild) { n.appendNamedMembers(name) },
			func(n *DiscordGuild, e *DiscordUser) { n.appendNamedMembers(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedDiscordMessages {
		if err := _q.loadDiscordMessages(ctx, query, nodes,
			func(n *DiscordGuild) { n.appendNamedDiscordMessages(name) },
			func(n *DiscordGuild, e *DiscordMessage) { n.appendNamedDiscordMessages(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedGuildChannels {
		if err := _q.loadGuildChannels(ctx, query, nodes,
			func(n *DiscordGuild) { n.appendNamedGuildChannels(name) },
			func(n *DiscordGuild, e *DiscordChannel) { n.appendNamedGuildChannels(name, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *DiscordGuildQuery) loadMembers(ctx context.Context, query *DiscordUserQuery, nodes []*DiscordGuild, init func(*DiscordGuild), assign func(*DiscordGuild, *DiscordUser)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uuid.UUID]*DiscordGuild)
	nids := make(map[uuid.UUID]map[*DiscordGuild]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(discordguild.MembersTable)
		s.Join(joinT).On(s.C(discorduser.FieldID), joinT.C(discordguild.MembersPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(discordguild.MembersPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(discordguild.MembersPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(uuid.UUID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*uuid.UUID)
				inValue := *values[1].(*uuid.UUID)
				if nids[inValue] == nil {
					nids[inValue] = map[*DiscordGuild]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*DiscordUser](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "members" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (_q *DiscordGuildQuery) loadDiscordMessages(ctx context.Context, query *DiscordMessageQuery, nodes []*DiscordGuild, init func(*DiscordGuild), assign func(*DiscordGuild, *DiscordMessage)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*DiscordGuild)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.DiscordMessage(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(discordguild.DiscordMessagesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.discord_guild_discord_messages
		if fk == nil {
			return fmt.Errorf(`foreign-key "discord_guild_discord_messages" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "discord_guild_discord_messages" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *DiscordGuildQuery) loadGuildChannels(ctx context.Context, query *DiscordChannelQuery, nodes []*DiscordGuild, init func(*DiscordGuild), assign func(*DiscordGuild, *DiscordChannel)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uuid.UUID]*DiscordGuild)
	nids := make(map[uuid.UUID]map[*DiscordGuild]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(discordguild.GuildChannelsTable)
		s.Join(joinT).On(s.C(discordchannel.FieldID), joinT.C(discordguild.GuildChannelsPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(discordguild.GuildChannelsPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(discordguild.GuildChannelsPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(uuid.UUID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*uuid.UUID)
				inValue := *values[1].(*uuid.UUID)
				if nids[inValue] == nil {
					nids[inValue] = map[*DiscordGuild]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*DiscordChannel](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "guild_channels" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (_q *DiscordGuildQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *DiscordGuildQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(discordguild.Table, discordguild.Columns, sqlgraph.NewFieldSpec(discordguild.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, discordguild.FieldID)
		for i := range fields {
			if fields[i] != discordguild.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *DiscordGuildQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(discordguild.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = discordguild.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *DiscordGuildQuery) ForUpdate(opts ...sql.LockOption) *DiscordGuildQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *DiscordGuildQuery) ForShare(opts ...sql.LockOption) *DiscordGuildQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// WithNamedMembers tells the query-builder to eager-load the nodes that are connected to the "members"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *DiscordGuildQuery) WithNamedMembers(name string, opts ...func(*DiscordUserQuery)) *DiscordGuildQuery {
	query := (&DiscordUserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedMembers == nil {
		_q.withNamedMembers = make(map[string]*DiscordUserQuery)
	}
	_q.withNamedMembers[name] = query
	return _q
}

// WithNamedDiscordMessages tells the query-builder to eager-load the nodes that are connected to the "discord_messages"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *DiscordGuildQuery) WithNamedDiscordMessages(name string, opts ...func(*DiscordMessageQuery)) *DiscordGuildQuery {
	query := (&DiscordMessageClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedDiscordMessages == nil {
		_q.withNamedDiscordMessages = make(map[string]*DiscordMessageQuery)
	}
	_q.withNamedDiscordMessages[name] = query
	return _q
}

// WithNamedGuildChannels tells the query-builder to eager-load the nodes that are connected to the "guild_channels"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *DiscordGuildQuery) WithNamedGuildChannels(name string, opts ...func(*DiscordChannelQuery)) *DiscordGuildQuery {
	query := (&DiscordChannelClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedGuildChannels == nil {
		_q.withNamedGuildChannels = make(map[string]*DiscordChannelQuery)
	}
	_q.withNamedGuildChannels[name] = query
	return _q
}

// DiscordGuildGroupBy is the group-by builder for DiscordGuild entities.
type DiscordGuildGroupBy struct {
	selector
	build *DiscordGuildQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *DiscordGuildGroupBy) Aggregate(fns ...AggregateFunc) *DiscordGuildGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *DiscordGuildGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DiscordGuildQuery, *DiscordGuildGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *DiscordGuildGroupBy) sqlScan(ctx context.Context, root *DiscordGuildQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DiscordGuildSelect is the builder for selecting fields of DiscordGuild entities.
type DiscordGuildSelect struct {
	*DiscordGuildQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *DiscordGuildSelect) Aggregate(fns ...AggregateFunc) *DiscordGuildSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *DiscordGuildSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DiscordGuildQuery, *DiscordGuildSelect](ctx, _s.DiscordGuildQuery, _s, _s.inters, v)
}

func (_s *DiscordGuildSelect) sqlScan(ctx context.Context, root *DiscordGuildQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
